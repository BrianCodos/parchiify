import React from 'react';
import SearchInterface from './SearchInterface';

const MainContent = () => {
  return (
    <div className="flex-1 bg-dark-primary overflow-auto">
      <SearchInterface />
    </div>
  );
};

export default MainContent; 